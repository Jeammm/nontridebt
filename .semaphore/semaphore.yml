version: v1.0
name: Python
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Test
    task:
      jobs:
        - name: pytest
          commands:
            - checkout
            - python -m pip install --upgrade pip
            - pip install -r requirements.txt
            - pytest
